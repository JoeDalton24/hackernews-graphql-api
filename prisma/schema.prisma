// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
  // shadowDatabaseUrl = env("postgres://woyuovwljneyfo:7a13889143e39bd00110c783f339c15e6f67ad29681e8c94bda5a5061d8733b2@ec2-54-157-79-121.compute-1.amazonaws.com:5432/d7unba4odgjung")
}

model Link {
  id          Int      @id @default(autoincrement())
  description String
  url         String   @unique
  createdAt   DateTime @default(now())
  postedBy    User?    @relation(fields: [postedById], references: [id])
  postedById  Int?
}

model User {
  id       Int    @id @default(autoincrement())
  name     String
  email    String @unique
  password String
  links    Link[]
}
